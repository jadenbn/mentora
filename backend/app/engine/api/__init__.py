"""The engine's two HTTP surfaces.

`learning` is the product path: grade a canvas, record the attempt. `dev` is
the dashboard and its synthetic-attempt controls, which are the only way to
see the engine at all -- it has no student-facing UI by design.
"""
