NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional, development only. Comma-separated hostnames allowed to request the
# dev server, needed to open it from another device on your network:
#   ALLOWED_DEV_ORIGINS=192.168.1.20
# Without it Next blocks that device's cross-origin requests for dev assets.
ALLOWED_DEV_ORIGINS=
